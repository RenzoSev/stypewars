import styled from 'styled-components';
import FormControlLabel from '@material-ui/core/FormControlLabel';

const GlobalFormControlLabel = styled(FormControlLabel)``;

export default GlobalFormControlLabel;
