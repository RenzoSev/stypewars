import styled from 'styled-components';
import FormControl from '@material-ui/core/FormControl';

const GlobalFormControl = styled(FormControl)`
  background: #ffffff0a;
  border: 1px solid #8080801f;
  border-radius: 8px;
`;

export default GlobalFormControl;
